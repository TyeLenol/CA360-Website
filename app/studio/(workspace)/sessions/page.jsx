import { StudioSessions } from '../../../../components/studio/StudioSessions';

export const metadata = { title: 'Sessions' };

export default function StudioSessionsRoute() {
  return <StudioSessions />;
}
