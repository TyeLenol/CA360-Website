import { ContactPage } from '../../components/contact/ContactPage';

export const metadata = {
  title: 'Contact',
  description: 'Start the conversation with Career Arcadia 360 — questions from students, mentors, schools, partners, and the wider CA360 community.',
};

export default function ContactRoute() {
  return <ContactPage />;
}
